# Windows Event Logs in SecOps SIEM

To search Windows Event Logs in SecOps SIEM, use the following `metadata.log_type` value:

```
metadata.log_type = "WINEVTLOG"
```

This is the indexed metadata field value required for querying Windows Event Log data in SecOps SIEM.
